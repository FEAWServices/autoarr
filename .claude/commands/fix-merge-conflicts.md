Merge origin default branch. Look for develop, dev and then main in that order
and then merge into the current branch and resolve the merge conflicts asking
the user to confirm which should take precendece with your recomendations and
why.

Then once conflicts are resolved commit and push.
